from setuptools import setup, find_packages

setup(
    name="RANSAC",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[],  # Add dependencies if needed
    description="RANSAC algorithms for perception",
    author="WEAP",
    python_requires=">=3.10",
)
